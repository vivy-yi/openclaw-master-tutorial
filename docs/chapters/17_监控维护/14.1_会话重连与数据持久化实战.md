# 14.1 会话重连与数据持久化实战

> **难度**：中级
> **预计阅读时间**：20 分钟
> **来源**：GitHub Issues [#60549](https://github.com/openclaw/openclaw/issues/60549)、[#60542](https://github.com/openclaw/openclaw/issues/60542)
> **版本**：v1.0 | 2026-04-06

---

## 一、问题概述

### 1.1 典型现象

```
┌────────────────────────────────────────────────────────────────┐
│                    会话重连问题场景                               │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  现象1：TUI会话重连后丢失                                     │
│    → 重连后 Agent 表示"不记得之前的对话"                       │
│    → 来源：Issue #60549                                        │
│                                                                │
│  现象2：持久化 session 数据陈旧                                │
│    → session 数据重启后未刷新，仍显示旧上下文                   │
│    → 来源：Issue #60542                                        │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

### 1.2 问题根因分析

| 现象 | 可能的根因 | 排查方向 |
|------|----------|---------|
| TUI 重连丢失 | sessionKey 映射丢失 | 检查会话路由配置 |
| TUI 重连丢失 | 内存中的 session 未持久化 | 检查 workspace 持久化 |
| 数据陈旧 | session 加载了旧快照 | 检查 session 初始化逻辑 |
| 数据陈旧 | 多实例写入冲突 | 检查并发写入 |

---

## 二、排查步骤

### 2.1 步骤一：确认 sessionKey 是否正确分配

```bash
# 查看当前所有活跃 session
openclaw sessions list

# 输出示例
session_key                       | last_active | messages
─────────────────────────────────────────────────────────────
main:telegram:6020964033          | 30s ago    | 12
isolated:cron:12345              | 5m ago     | 3
```

**诊断要点**：
- 重连后 `sessionKey` 是否与之前一致？
- 如果不一致 → 说明会话路由映射有问题
- 如果一致但消息为空 → 说明 session 内容未持久化

### 2.2 步骤二：检查会话持久化状态

```bash
# 检查 workspace 下的 session 文件
ls ~/.openclaw/workspace/sessions/

# 检查 session 文件更新时间
ls -la ~/.openclaw/workspace/sessions/*.json | head -10

# 检查 session 文件内容（关键字段）
cat ~/.openclaw/workspace/sessions/<session_id>.json | jq '{
  updated_at: .updated_at,
  message_count: .messages | length,
  last_message: .messages[-1] | .content[0:100]
}'
```

**正常状态**：
```json
{
  "updated_at": "2026-04-06T02:30:00Z",
  "message_count": 15,
  "last_message": "好的，我来帮你分析..."
}
```

**异常状态（陈旧数据）**：
```json
{
  "updated_at": "2026-04-05T22:00:00Z",   // ← 更新时间是一天前！
  "message_count": 3,                        // ← 消息数明显不对
  "last_message": "你好，请问你需要什么帮助？"
}
```

### 2.3 步骤三：检查重连后的 session 恢复流程

```bash
# 查看 Gateway 日志（重连相关）
openclaw gateway logs 2>&1 | grep -E "session|reconnect|restore" | tail -30

# 正常日志示例
[INFO] session: restoring session_key=main:telegram:6020964033
[INFO] session: loaded 15 messages from persistence
[INFO] session: restore complete

# 异常日志示例
[INFO] session: restoring session_key=main:telegram:6020964033
[WARN] session: persistence file missing, starting fresh
[INFO] session: created new session
```

---

## 三、修复方案

### 3.1 方案一：修复 session 持久化配置

```json
// openclaw.json
{
  "sessions": {
    "persistence": {
      "enabled": true,
      "path": "~/.openclaw/workspace/sessions",
      "interval_seconds": 30,
      "on_exit": "flush"    // ← 关键：退出时强制持久化
    }
  }
}
```

**验证修复**：
```bash
# 重启 Gateway
openclaw gateway restart

# 触发一条消息
echo "test" | openclaw

# 检查 session 文件更新时间
stat ~/.openclaw/workspace/sessions/main.json | grep Modify
```

### 3.2 方案二：处理多实例写入冲突

**问题**：多个 OpenClaw 实例写入同一 session 文件，导致最后写入覆盖。

```bash
# 检查是否有多个 Gateway 在运行
ps aux | grep openclaw | grep -v grep

# 如果有多个，关闭多余的
# 或通过配置文件隔离实例
```

**隔离配置**：
```json
{
  "sessions": {
    "instance_id": "gateway-1",   // ← 每个实例唯一 ID
    "persistence": {
      "enabled": true,
      "locking": true             // ← 启用文件锁
    }
  }
}
```

---

## 四、预防措施

### 4.1 配置自动持久化

```
每天定时任务：检查 session 文件更新时间
  → 若超过 1 小时未更新 → 触发警告
  → 若超过 24 小时 → 触发清理
```

### 4.2 配置 cron 定期保存

```yaml
# openclaw cron 配置
- name: session-health-check
  schedule: "0 */2 * * *"   # 每2小时
  action: |
    检查 session 持久化状态
    异常时推送告警
```

---

## 五、相关配置项速查

| 配置项 | 默认值 | 说明 |
|--------|-------|------|
| `sessions.persistence.enabled` | `true` | 启用持久化 |
| `sessions.persistence.interval` | `60s` | 持久化间隔 |
| `sessions.persistence.on_exit` | `flush` | 退出时持久化 |
| `sessions.instance_id` | `gateway` | 实例唯一标识 |

---

## 六、延伸问题

### Q1: 重连后 memory 也丢失了？

memory（MEMORY.md）不依赖 session，重连后仍然有效。如果也丢失，检查：
- MEMORY.md 文件是否存在
- workspace 路径是否正确配置

### Q2: 如何强制恢复旧 session？

```bash
# 备份当前 session
cp ~/.openclaw/workspace/sessions/main.json ~/.openclaw/workspace/sessions/main.json.bak

# 从备份恢复
cp ~/.openclaw/workspace/sessions/main.json.bak ~/.openclaw/workspace/sessions/main.json

# 重启
openclaw gateway restart
```

---

## 七、参考链接

- Issue [#60549 - TUI会话重连后丢失](https://github.com/openclaw/openclaw/issues/60549)
- Issue [#60542 - 持久化main session数据陈旧](https://github.com/openclaw/openclaw/issues/60542)
- [sessions_list 工具文档](../03_发出指令/3.9_session.md)
- [会话上下文文档](../03_发出指令/3.11_session_context.md)

---

*本教程基于 GitHub Issues 实战经验整理*
