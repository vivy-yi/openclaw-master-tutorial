# OpenClaw 截图完全指南

> 解决教程制作中的各种截图需求

## 概述

制作 OpenClaw 教程时，截图是必不可少的一环。本文档介绍如何使用 OpenClaw 的各种截图能力，满足不同场景的需求。

## 截图方案总览

| 场景 | 推荐方案 | 工具 |
|------|----------|------|
| 截图自己电脑屏幕 | Nodes 设备截图 | 手机/电脑 screen record |
| 截取网页/网页应用 | browser 工具 | OpenClaw 内置 |
| 分析他人截图 | image 工具 | AI 分析 |
| 生成 AI 配图 | 第三方 API | DALL-E/Midjourney |
| 截图代码/代码高亮 | 代码截图服务 | ray.so 等 |

---

## 1. 浏览器控制截图（内置）

OpenClaw 内置 `browser` 工具，支持多种截图模式。

### 1.1 基本用法

```python
# 全页截图
browser(action=screenshot, url="https://openclaw.ai")

# 指定区域截图
browser(action=screenshot, selector="#main-content")

# 带延迟截图（等待 JS 加载）
browser(action=screenshot, url="https://openclaw.ai", delayMs=3000)
```

### 1.2 高级选项

```python
# 滚动截图（长页面）
browser(action=screenshot, url="https://long-page.com", fullPage=true)

# 元素截图
browser(action=screenshot, element=".modal-content")

# 自定义尺寸
browser(action=screenshot, url="https://openclaw.ai", width=1920, height=1080)
```

### 1.3 CDP 协议（无头浏览器）

```python
# 使用 CDP 进行更高级的控制
browser(
    action=screenshot,
    url="https://openclaw.ai",
    CDP=true,
    options={
        "format": "png",
        "quality": 100,
        "captureBeyondViewport": true
    }
)
```

---

## 2. Nodes 设备截图（移动端）

配对的移动设备可以远程拍照/截图。

### 2.1 屏幕截图

```python
# 获取手机屏幕截图
nodes(action=screen_record, node="我的iPhone", duration="5s")

# 拍照
nodes(action=camera_snap, node="我的iPhone", facing="back")
```

### 2.2 屏幕录制

```python
# 录屏（用于捕捉操作过程）
nodes(
    action=screen_record,
    node="我的iPhone",
    duration="30s",
    fps=30,
    includeAudio=true
)
```

### 2.3 定期截图任务

可以设置定时任务，定期截取设备屏幕：

```python
cron.add(
    name="每日屏幕截图",
    schedule="0 9 * * *",  # 每天 9 点
    payload={
        "kind": "agentTurn",
        "message": "截取手机屏幕并保存到教程目录"
    }
)
```

---

## 3. 图片分析（AI 理解）

OpenClaw 内置 `image` 工具，可以分析任意图片内容。

### 3.1 分析截图

```python
# 分析图片内容
image(
    action=analyze,
    image="/path/to/screenshot.png",
    prompt="描述这个界面包含哪些元素"
)

# 提取 UI 信息
image(
    action=analyze,
    image="/path/to/screenshot.png",
    prompt="提取界面中的按钮名称和交互元素"
)
```

### 3.2 理解操作流程

```python
# 分析操作步骤
image(
    action=analyze,
    image="/path/to/install-step.png",
    prompt="这个截图展示的是安装过程的哪一步？"
)
```

---

## 4. 第三方服务集成

### 4.1 SnapRender（网页截图 API）

```python
# 使用 SnapRender 进行网页截图
web_fetch(url="https://api.snap-render.com/screenshot", params={
    "url": "https://openclaw.ai",
    "width": 1920,
    "height": 1080,
    "format": "png"
})
```

### 4.2 Puppeteer/Playwright（复杂交互）

对于需要先交互再截图的场景：

```javascript
// puppeteer-screenshot.js
const puppeteer = require('puppeteer');

async function screenshot() {
    const browser = await puppeteer.launch();
    const page = await browser.newPage();
    
    // 登录后的页面
    await page.goto('https://app.openclaw.ai/dashboard');
    await page.type('#username', 'user');
    await page.type('#password', 'pass');
    await page.click('#login-btn');
    await page.waitForNavigation();
    
    // 截图
    await page.screenshot({ path: 'dashboard.png', fullPage: true });
    
    await browser.close();
}

screenshot();
```

### 4.3 代码高亮截图（ray.so）

```python
# 调用 ray.so API 生成代码截图
web_fetch(
    url="https://ray.so/api/image",
    params={
        "code": "your code here",
        "theme": "dracula",
        "padding": 32,
        "language": "python"
    }
)
```

---

## 5. AI 生成配图

### 5.1 使用 DALL-E 生成示意图

```python
# 通过 OpenAI API 生成配图
openai.images.generate(
    prompt="A clean, modern UI diagram showing OpenClaw architecture with agents, tools, and channels as interconnected nodes",
    size="1024x1024",
    n=1
)
```

### 5.2 使用 Midjourney

```python
# 发送 prompt 到 Discord
message.send(
    channel="midjourney",
    message="/imagine prompt: OpenClaw tutorial illustration, screenshot style, clean UI, minimalist --ar 16:9"
)
```

---

## 6. 自动化截图工作流

### 6.1 定时网页截图

```python
# 创建定时任务
cron.add(
    name="每日文档截图",
    schedule="0 6 * * *",  # 每天 6 点
    payload={
        "kind": "agentTurn",
        "message": """
        1. 访问 https://docs.openclaw.ai
        2. 截取首页截图
        3. 保存到 /tutorials/assets/
        4. 更新文档索引
        """
    }
)
```

### 6.2 群里 URL 自动截图

在群里发送 URL 自动截图：

```python
# 监听消息
if message.match(regex=r'https?://\S+'):
    # 提取 URL
    url = message.extract_url()
    # 截图
    screenshot = browser(action=screenshot, url=url)
    # 返回截图
    message.reply(image=screenshot)
```

### 6.3 截图后自动标注

```python
# 截图 + AI 标注
screenshot = browser(action=screenshot, url=url)
annotated = image(
    action=edit,
    image=screenshot,
    prompt="在关键位置添加箭头和文字标注"
)
```

---

## 7. 最佳实践

### 7.1 截图规范

| 类型 | 分辨率 | 格式 | 建议 |
|------|--------|------|------|
| UI 截图 | 1920x1080 | PNG | 高清优先 |
| 代码截图 | 宽度 800px | PNG | 使用 ray.so |
| 流程图 | 1920x1080 | PNG/SVG | 使用 Mermaid |
| AI 配图 | 1024x1024 | PNG | 16:9 横版 |

### 7.2 命名规范

```
{章节号}_{场景名}_{序号}.png
例如：02_setup_login_01.png
```

### 7.3 存储位置

```
/tutorials/
├── assets/
│   ├── screenshots/    # 手动截图
│   ├── ai-generated/  # AI 生成
│   └── diagrams/      # 流程图
└── docs/
    └── 01_overview/
        └── media/      # 章节专用
```

---

## 8. 常见问题

### Q1: 截图模糊怎么办？

**解决方案**：
- 使用 `deviceScaleFactor: 2` 提高分辨率
- 优先使用 PNG 而非 JPEG

### Q2: 动态内容截图不完整？

**解决方案**：
- 增加 `delayMs` 等待加载
- 使用 `waitForSelector` 等待特定元素

### Q3: 需要登录后的截图？

**解决方案**：
- 使用 Playwright 配合 cookie
- 或通过 API 获取登录状态

---

## 9. 相关资源

- [OpenClaw Browser 官方文档](https://docs.openclaw.ai/docs/gateway/browser)
- [SnapRender 服务](https://snap-render.com)
- [ray.so 代码截图](https://ray.so)

---

*本文档由 OpenClaw 自动化教程系统生成*