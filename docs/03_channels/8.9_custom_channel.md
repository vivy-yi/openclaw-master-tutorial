# 8.6 自定义渠道

## 本节目标
- 掌握自定义渠道开发
- 理解渠道接口规范
- 完成自定义渠道实战

---

## 8.6.1 自定义渠道概述

### 为什么需要自定义

```
自定义渠道场景：

1. 特殊平台
   ├── 内部系统
   ├── 专有协议
   └── 定制需求

2. 私有部署
   ├── 内网系统
   ├── 私有云
   └── 定制协议

3. 创新接入
   ├── 新兴平台
   ├── IoT 设备
   └── 硬件交互
```

---

## 8.6.2 架构设计

### 接口规范

```
自定义渠道需要实现：

1. 消息接收
   └── receive(message) → Message

2. 消息发送
   └── send(response) → Result

3. 事件处理
   └── handle(event) → void

4. 资源清理
   └── cleanup() → void
```

### 基础结构

```python
from openclaw import Channel

class CustomChannel(Channel):
    def __init__(self, config):
        super().__init__(config)
        self.client = None

    async def connect(self):
        """建立连接"""
        pass

    async def receive(self):
        """接收消息"""
        pass

    async def send(self, message):
        """发送消息"""
        pass

    async def cleanup(self):
        """清理资源"""
        pass
```

---

## 8.6.3 开发示例

### 简单 HTTP 渠道

```python
from openclaw import Channel
from aiohttp import web

class HttpChannel(Channel):
    """HTTP 回调渠道"""

    def __init__(self, config):
        super().__init__(config)
        self.port = config.get('port', 8080)
        self.app = web.Application()

    async def setup(self):
        """设置路由"""
        self.app.router.add_post('/webhook', self.handle_webhook)

    async def handle_webhook(self, request):
        """处理 Webhook 请求"""
        data = await request.json()
        message = self.parse_message(data)
        response = await self.process(message)
        return web.json_response(response)

    def parse_message(self, data):
        """解析消息"""
        return Message(
            msg_id=data.get('id'),
            content=data.get('content'),
            user=User(id=data['user']['id']),
            channel='custom_http'
        )

    async def send(self, message):
        """发送回复"""
        # 实现发送逻辑
        pass
```

### WebSocket 渠道

```python
import asyncio
import websockets

class WebSocketChannel(Channel):
    """WebSocket 渠道"""

    async def connect(self):
        """建立 WebSocket 连接"""
        self.ws_url = self.config.get('ws_url')
        self.websocket = await websockets.connect(self.ws_url)
        asyncio.create_task(self.listen())

    async def listen(self):
        """监听消息"""
        async for message in self.websocket:
            await self.handle_message(message)

    async def send(self, message):
        """发送消息"""
        await self.websocket.send(json.dumps(message))
```

---

## 8.6.4 配置注册

### 注册渠道

```json
{
  "channels": {
    "custom": {
      "enabled": true,
      "type": "custom",
      "module": "my_channel.custom_channel",
      "class": "CustomChannel",
      "config": {
        "param1": "value1",
        "param2": "value2"
      }
    }
  }
}
```

### 模块结构

```
自定义渠道模块结构：

my_channel/
├── __init__.py
├── custom_channel.py
└── config.json
```

---

## 8.6.5 消息协议

### 消息转换

```python
def to_external_format(message):
    """转换为外部格式"""
    return {
        'msg_id': message.msg_id,
        'content': message.content,
        'user': {
            'id': message.user.id,
            'name': message.user.name
        },
        'timestamp': message.timestamp
    }

def from_external_format(data):
    """从外部格式转换"""
    return Message(
        msg_id=data['id'],
        content=data['text'],
        user=User(
            id=data['from']['id'],
            name=data['from']['name']
        ),
        channel='custom'
    )
```

---

## 8.6.6 测试调试

### 本地测试

```python
# 测试自定义渠道
import asyncio
from my_channel import CustomChannel

async def test():
    config = {'param': 'value'}
    channel = CustomChannel(config)

    # 测试连接
    await channel.connect()

    # 测试发送
    response = await channel.send(Message(...))
    print(response)

    # 清理
    await channel.cleanup()

asyncio.run(test())
```

### 调试技巧

```
调试方法：

1. 日志输出
   → 添加详细日志
   → 跟踪消息流程

2. 断点调试
   → 使用 IDE 断点
   → 逐步执行

3. 模拟测试
   → 构造测试数据
   → 验证转换逻辑

4. 错误处理
   → 完善异常捕获
   → 友好的错误信息
```

---

## 8.6.7 常见问题

### Q1: 消息解析失败

```
问题：无法解析消息

解决：
1. 检查消息格式
2. 验证字段映射
3. 添加日志跟踪
4. 完善错误处理
```

### Q2: 连接失败

```
问题：无法建立连接

解决：
1. 检查连接参数
2. 验证网络可达
3. 检查认证信息
4. 添加重试机制
```

### Q3: 消息丢失

```
问题：消息没有处理

解决：
1. 添加消息确认
2. 实现重试机制
3. 记录处理日志
4. 监控队列状态
```

---

## 本节小结

1. **自定义渠道**：满足特殊需求
2. **开发接口**：receive、send、handle
3. **协议转换**：外部格式 ↔ 内部格式
4. **测试**：本地测试 + 调试

---

## 课后思考

1. 你需要什么类型的自定义渠道？
2. 如何设计消息协议？
3. 如何保证消息不丢失？
