# 4.1 模型提供商概览

## 本节目标
- 了解 OpenClaw 支持的模型提供商
- 掌握选择模型的方法
- 理解不同提供商的特点

---

## 4.1.1 CLI 配置模型

### 常用命令

OpenClaw 提供了丰富的 CLI 命令来管理模型配置：

```bash
# 查看当前模型配置
openclaw models list

# 添加模型配置
openclaw models add <provider> --api-key <key>

# 设置默认模型
openclaw models set-default <model-id>

# 测试模型连接
openclaw models test <model-id>

# 查看可用模型
openclaw models available
```

### 配置流程图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          模型配置流程                                           │
└─────────────────────────────────────────────────────────────────────────────────┘

                                    开始
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  1. 选择模型提供商    │
                          │  ┌───────────────┐  │
                          │  │ • OpenAI     │  │
                          │  │ • Anthropic  │  │
                          │  │ • DeepSeek   │  │
                          │  │ • Ollama     │  │
                          │  └───────────────┘  │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  2. 获取 API Key     │
                          │  ┌───────────────┐  │
                          │  │ 官网注册账号  │  │
                          │  │ 创建 API Key │  │
                          │  │ 充值/购买    │  │
                          │  └───────────────┘  │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  3. 配置 OpenClaw   │
                          │  ┌───────────────┐  │
                          │  │ CLI 配置      │  │
                          │  │ JSON 配置     │  │
                          │  └───────────────┘  │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  4. 测试连接         │
                          │  openclaw models    │
                          │  test <model-id>   │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  5. 设为默认         │
                          │  openclaw models    │
                          │  set-default        │
                          └───────────┬───────────┘
                                      │
                                      ▼
                              ┌───────────────┐
                              │   配置完成 ✅   │
                              └───────────────┘
```

### CLI 配置示例

#### 添加 OpenAI 模型

```bash
# 方式一：命令行直接配置
openclaw models add openai \
  --api-key sk-xxxxx \
  --default-model gpt-4o

# 方式二：环境变量
export OPENAI_API_KEY=sk-xxxxx

# 测试连接
openclaw models test openai
```

#### 添加 Anthropic (Claude) 模型

```bash
# 添加 Claude 模型
openclaw models add anthropic \
  --api-key sk-ant-xxxxx \
  --default-model claude-sonnet-4-20250514

# 测试连接
openclaw models test anthropic
```

#### 添加 DeepSeek 模型

```bash
# 添加 DeepSeek 模型
openclaw models add deepseek \
  --api-key sk-xxxxx \
  --default-model deepseek-chat

# 测试连接
openclaw models test deepseek
```

#### 添加 Ollama 本地模型

```bash
# 确保 Ollama 已启动
ollama serve

# 添加本地模型
openclaw models add ollama \
  --base-url http://localhost:11434 \
  --default-model llama3

# 测试连接
openclaw models test ollama
```

### JSON 配置文件

除了 CLI，你也可以直接编辑配置文件：

```json
{
  "models": {
    "providers": {
      "openai": {
        "apiKey": "sk-xxxxx",
        "defaultModel": "gpt-4o",
        "baseUrl": "https://api.openai.com/v1"
      },
      "anthropic": {
        "apiKey": "sk-ant-xxxxx",
        "defaultModel": "claude-sonnet-4-20250514"
      },
      "deepseek": {
        "apiKey": "sk-xxxxx",
        "defaultModel": "deepseek-chat",
        "baseUrl": "https://api.deepseek.com/v1"
      },
      "ollama": {
        "baseUrl": "http://localhost:11434",
        "defaultModel": "llama3"
      }
    },
    "fallback": ["gpt-4o-mini", "claude-3-haiku"]
  }
}
```

### 查看当前配置

```bash
# 列出所有配置的模型
openclaw models list

# 查看默认模型
openclaw models current

# 查看模型状态
openclaw status | grep model
```

### 多模型切换

OpenClaw 支持配置多个模型，并根据不同场景灵活切换。

#### 切换默认模型

```bash
# 切换到不同的默认模型
openclaw models set-default gpt-4o
openclaw models set-default claude-sonnet-4-20250514
openclaw models set-default deepseek-chat
openclaw models set-default llama3
```

#### 会话级模型切换

```bash
# 在对话中切换模型
> @gpt-4o 请解释这个概念

> @claude-3-5-sonnet 帮我写代码

> @deepseek-chat 翻译这段文字
```

#### 运行时动态切换

```bash
# 使用 --model 参数指定模型
openclaw chat --model gpt-4o --message "你好"

openclaw chat --model claude-opus --message "你好"

openclaw chat --model deepseek-coder --message "写一个排序算法"
```

#### 模型优先级配置

在配置文件中设置模型优先级和自动切换规则：

```json
{
  "models": {
    "priority": {
      "coding": ["claude-sonnet", "gpt-4o", "deepseek-coder"],
      "writing": ["claude-haiku", "gpt-4o-mini", "deepseek-chat"],
      "analysis": ["claude-opus", "gpt-4o", "deepseek-chat"]
    },
    "fallback": {
      "primary": "claude-sonnet-4-20250514",
      "secondary": "gpt-4o-mini",
      "tertiary": "deepseek-chat"
    },
    "autoSelect": {
      "enabled": true,
      "rules": [
        { "keyword": "code", "model": "claude-sonnet" },
        { "keyword": "写代码", "model": "deepseek-coder" },
        { "keyword": "翻译", "model": "gpt-4o-mini" }
      ]
    }
  }
}
```

#### 多模型配置流程图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          多模型切换配置流程                                      │
└─────────────────────────────────────────────────────────────────────────────────┘

                                    开始
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  1. 配置多个模型    │
                          │  openclaw models    │
                          │  add <provider1>    │
                          │  add <provider2>    │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  2. 设置优先级       │
                          │  JSON 配置:         │
                          │  models.priority    │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  3. 配置自动切换     │
                          │  models.autoSelect  │
                          │  根据关键词自动选择  │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  4. 配置降级策略     │
                          │  models.fallback    │
                          │  primary/secondary  │
                          └───────────┬───────────┘
                                      │
                                      ▼
                          ┌───────────────────────┐
                          │  5. 测试切换         │
                          │  @modelName 或      │
                          │  --model 参数        │
                          └───────────┬───────────┘
                                      │
                                      ▼
                              ┌───────────────┐
                              │   配置完成 ✅   │
                              └───────────────┘
```

#### 按场景使用不同模型

```bash
# 编程场景 - 使用 Claude
openclaw chat --model claude-sonnet-4-20250514 --message "帮我写一个 Python Web 服务器"

# 日常对话 - 使用 DeepSeek
openclaw chat --model deepseek-chat --message "今天天气怎么样"

# 长文本分析 - 使用 Kimi
openclaw chat --model moonshot-kimi --message "分析这篇论文的主要观点"

# 快速翻译 - 使用 Haiku
openclaw chat --model claude-3-haiku --message "把这段话翻译成英文"
```

#### 模型状态查看

```bash
# 查看所有可用模型及其状态
openclaw models available

# 查看当前激活的模型
openclaw models active

# 查看模型使用统计
openclaw models stats
```

---

## 4.1.1 支持的模型提供商

OpenClaw 支持 **30+** 模型提供商：

```
┌─────────────────────────────────────────────────────────────────────┐
│                    模型提供商汇总                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                              │
│  国际厂商                                                     │
│  ├── OpenAI (GPT-4o, GPT-4o-mini, o1, o3-mini)           │
│  ├── Anthropic (Claude 3.5 Sonnet, Claude 3 Haiku)         │
│  ├── Google (Gemini 2.0 Flash, Pro)                       │
│  ├── Mistral (Mistral Large, Codestral)                   │
│  └── xAI (Grok)                                          │
│                                                              │
│  国产厂商 ⭐                                                 │
│  ├── DeepSeek (V3, Chat, Coder)                          │
│  ├── Moonshot (Kimi, Kimi Coder)                         │
│  ├── MiniMax (abab6.5s)                                   │
│  ├── 智谱 (GLM-4, GLM-4V)                                 │
│  ├── 百度 (ERNIE 4.0, 3.5)                               │
│  ├── 阿里 (Qwen 2.5, VL)                                   │
│  └── 腾讯 (Hunyuan)                                        │
│                                                              │
│  本地部署                                                     │
│  ├── Ollama (Llama 3, Qwen 2, Mistral)                   │
│  └── vLLM (高吞吐量本地部署)                                │
│                                                              │
│  聚合平台                                                     │
│  ├── OpenRouter (聚合 100+ 模型)                           │
│  ├── LiteLLM (统一网关)                                    │
│  └── Cloudflare AI Gateway                                 │
│                                                              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4.1.2 按场景选择

### 推荐场景

| 场景 | 推荐模型 | 原因 |
|-----|---------|------|
| **日常对话** | DeepSeek Chat | 性价比高 |
| **长文本处理** | Kimi (200万字) | 超长上下文 |
| **代码生成** | Claude 3.5 | 编程能力强 |
| **中文任务** | GLM-4 | 中文优化 |
| **本地部署** | Ollama Llama3 | 隐私优先 |

### 成本对比

```
模型成本对比（按 1M tokens）：

| 模型 | 输入 | 输出 | 推荐场景 |
|-----|------|------|---------|
| DeepSeek V3 | $0.27 | $1.10 | 日常使用 |
| Kimi | $0.12 | $1.10 | 长文本 |
| Claude 3.5 | $3.00 | $15.00 | 高级任务 |
| GPT-4o | $2.50 | $10.00 | 综合能力 |
| GLM-4 | $0.07 | $0.70 | 中文任务 |
```

---

## 4.1.3 国际厂商详解

### OpenAI

```
┌─────────────────────────────────────────────┐
│              OpenAI                          │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── GPT-4o (最新旗舰)                     │
│  ├── GPT-4o-mini (性价比)                  │
│  ├── o1 (推理)                            │
│  └── o3-mini (推理性价比)                  │
│                                             │
│  特点：                                     │
│  ✅ 综合能力强                              │
│  ✅ 生态完善                               │
│  ✅ 持续更新                               │
│                                             │
│  官网：platform.openai.com                │
│                                             │
└─────────────────────────────────────────────┘
```

### Anthropic (Claude)

```
┌─────────────────────────────────────────────┐
│           Anthropic (Claude)                 │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── Claude 3.5 Sonnet (旗舰)              │
│  ├── Claude 3 Haiku (性价比)              │
│  └── Claude 3 Opus (最强)                 │
│                                             │
│  特点：                                     │
│  ✅ 编程能力强                              │
│  ✅ 安全性高                               │
│  ✅ 输出质量高                             │
│                                             │
│  官网：console.anthropic.com              │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 4.1.4 国产厂商详解

### DeepSeek

```
┌─────────────────────────────────────────────┐
│               DeepSeek                       │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── DeepSeek V3 (最新旗舰)               │
│  ├── DeepSeek Chat (对话)                  │
│  └── DeepSeek Coder (编程)                │
│                                             │
│  特点：                                     │
│  ✅ 性价比极高 ⭐                           │
│  ✅ 推理能力强                              │
│  ✅ 开源模型                               │
│                                             │
│  价格：                                     │
│  输入：$0.27/M tokens                      │
│  输出：$1.10/M tokens                     │
│                                             │
│  官网：platform.deepseek.com               │
│                                             │
└─────────────────────────────────────────────┘
```

### Moonshot (Kimi)

```
┌─────────────────────────────────────────────┐
│            Moonshot (Kimi)                 │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── Kimi (200万字上下文) ⭐               │
│  └── Kimi Coder (编程)                    │
│                                             │
│  特点：                                     │
│  ✅ 超长上下文                              │
│  ✅ 中文理解好                              │
│  ✅ 长文本处理强                            │
│                                             │
│  价格：                                     │
│  输入：$0.12/M tokens                      │
│  输出：$1.10/M tokens                     │
│                                             │
│  官网：platform.moonshot.cn               │
│                                             │
└─────────────────────────────────────────────┘
```

### 智谱 (GLM)

```
┌─────────────────────────────────────────────┐
│              智谱 (GLM)                     │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── GLM-4 (旗舰)                          │
│  └── GLM-4V (多模态)                       │
│                                             │
│  特点：                                     │
│  ✅ 中文优化                               │
│  ✅ 多模态能力                             │
│  ✅ 性价比高                               │
│                                             │
│  官网：open.bigmodel.cn                   │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 4.1.5 本地部署

### Ollama

```
┌─────────────────────────────────────────────┐
│               Ollama                        │
├─────────────────────────────────────────────┤
│                                             │
│  推荐模型：                                   │
│  ├── Llama 3.1 (70B)                      │
│  ├── Qwen 2.5 (72B)                       │
│  └── Mistral (7B)                          │
│                                             │
│  特点：                                     │
│  ✅ 隐私优先                               │
│  ✅ 无需联网                               │
│  ✅ 零 API 费用                            │
│                                             │
│  硬件要求：                                 │
│  推荐：32GB+ 内存                          │
│  最低：16GB 内存                           │
│                                             │
│  官网：ollama.com                         │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 4.1.6 聚合平台

### OpenRouter

```
┌─────────────────────────────────────────────┐
│             OpenRouter                      │
├─────────────────────────────────────────────┤
│                                             │
│  特点：                                     │
│  ├── 聚合 100+ 模型                        │
│  ├── 统一 API 接口                          │
│  ├── 按需付费                              │
│  └── 内置 Claude/GPT                       │
│                                             │
│  官网：openrouter.ai                      │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 4.1.7 选择建议

### 新手推荐

| 优先级 | 模型 | 理由 |
|-------|------|------|
| ⭐⭐⭐ | DeepSeek | 性价比最高 |
| ⭐⭐ | Kimi | 中文友好 |
| ⭐ | OpenAI | 能力最强 |

### 进阶用户

| 场景 | 模型 |
|-----|------|
| 编程 | Claude 3.5 Sonnet |
| 长文本 | Kimi |
| 综合 | GPT-4o |

### 企业用户

| 需求 | 模型 |
|-----|------|
| 成本优先 | DeepSeek |
| 性能优先 | Claude 3.5 |
| 合规优先 | 国产模型 |

---

## 本节小结

1. **30+ 模型商**：国际厂商、国产厂商、本地部署、聚合平台
2. **推荐选择**：日常用 DeepSeek，长文本用 Kimi
3. **性价比**：DeepSeek > GLM > Kimi > GPT/Claude
4. **隐私**：Ollama 本地部署

---

## 课后思考

1. 你选择了哪个模型？为什么？
2. 你的使用场景是什么？
3. 成本是你考虑的主要因素吗？
