# 7.8 Agent 渠道绑定

## 本节目标
- 掌握 Agent 与渠道的绑定配置
- 理解路由规则
- 实现多渠道分发

---

## 7.8.1 绑定概念

### 什么是绑定

```
┌─────────────────────────────────────────────────────────────┐
│                     绑定 (Binding)                           │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Binding = 消息路由规则                                     │
│  将 inbound 消息分发到指定的 Agent                          │
│                                                              │
│  用户消息 → Gateway → Binding → Agent                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

![群组多Agent](./.assets/diagrams/24_group_multi_agent.png)



### 绑定组件

| 组件 | 说明 |
|-----|------|
| `agentId` | 目标 Agent ID |
| `channel` | 渠道类型 |
| `accountId` | 渠道账户 |
| `peer` | 对端 (DM/群组) |

---

## 7.8.2 绑定配置

### 基础配置

```json
{
  "bindings": [
    {
      "agentId": "main",
      "match": {
        "channel": "feishu"
      }
    }
  ]
}
```

### 多渠道绑定

```json
{
  "bindings": [
    {
      "agentId": "work",
      "match": {
        "channel": "discord"
      }
    },
    {
      "agentId": "social",
      "match": {
        "channel": "telegram"
      }
    }
  ]
}
```

---

## 7.8.3 路由优先级

### 优先级规则

```
路由匹配优先级（最具体优先）：
1. peer (精确匹配 DM/群组 ID)
2. parentPeer (线程继承)
3. guildId + roles (Discord 角色路由)
4. guildId (Discord 服务器)
5. teamId (Slack)
6. accountId (账户匹配)
7. channel (渠道级别)
8. default (默认 Agent)
```

### 精确匹配示例

```json
{
  "bindings": [
    {
      "agentId": "opus",
      "match": {
        "channel": "whatsapp",
        "peer": {
          "kind": "direct",
          "id": "+15551230001"
        }
      }
    },
    {
      "agentId": "chat",
      "match": {
        "channel": "whatsapp"
      }
    }
  ]
}
```

---

## 7.8.4 多渠道多账户

### Discord 多 Bot 绑定

```json
{
  "agents": {
    "list": [
      { "id": "main", "workspace": "~/.openclaw/workspace-main" },
      { "id": "coding", "workspace": "~/.openclaw/workspace-coding" }
    ]
  },
  "bindings": [
    { "agentId": "main", "match": { "channel": "discord", "accountId": "default" } },
    { "agentId": "coding", "match": { "channel": "discord", "accountId": "coding" } }
  ],
  "channels": {
    "discord": {
      "accounts": {
        "default": {
          "token": "DISCORD_BOT_TOKEN_MAIN"
        },
        "coding": {
          "token": "DISCORD_BOT_TOKEN_CODING"
        }
      }
    }
  }
}
```

### Telegram 多 Bot 绑定

```json
{
  "bindings": [
    { "agentId": "main", "match": { "channel": "telegram", "accountId": "default" } },
    { "agentId": "alerts", "match": { "channel": "telegram", "accountId": "alerts" } }
  ],
  "channels": {
    "telegram": {
      "accounts": {
        "default": { "botToken": "123456:ABC..." },
        "alerts": { "botToken": "987654:XYZ..." }
      }
    }
  }
}
```

### WhatsApp 多号码绑定

```bash
# 登录多个账户
openclaw channels login --channel whatsapp --account personal
openclaw channels login --channel whatsapp --account biz
```

```json
{
  "agents": {
    "list": [
      { "id": "home", "workspace": "~/.openclaw/workspace-home" },
      { "id": "work", "workspace": "~/.openclaw/workspace-work" }
    ]
  },
  "bindings": [
    { "agentId": "home", "match": { "channel": "whatsapp", "accountId": "personal" } },
    { "agentId": "work", "match": { "channel": "whatsapp", "accountId": "biz" } }
  ]
}
```

---

## 7.8.5 群组绑定

### Discord 群组

```json
{
  "bindings": [
    {
      "agentId": "dev",
      "match": {
        "channel": "discord",
        "guildId": "123456789012345678"
      }
    }
  ],
  "channels": {
    "discord": {
      "groupPolicy": "allowlist",
      "guilds": {
        "123456789012345678": {
          "channels": {
            "general": { "allow": true },
            "dev": { "allow": true }
          }
        }
      }
    }
  }
}
```

### WhatsApp 群组

```json
{
  "bindings": [
    {
      "agentId": "family",
      "match": {
        "channel": "whatsapp",
        "peer": {
          "kind": "group",
          "id": "120363999999999999@g.us"
        }
      }
    }
  ]
}
```

---

## 7.8.6 完整配置示例

### 微信/Telegram/WhatsApp 多渠道

```json
{
  "agents": {
    "list": [
      {
        "id": "chat",
        "name": "日常助手",
        "workspace": "~/.openclaw/workspace-chat",
        "model": "anthropic/claude-sonnet-4-5"
      },
      {
        "id": "opus",
        "name": "深度工作",
        "workspace": "~/.openclaw/workspace-opus",
        "model": "anthropic/claude-opus-4-6"
      }
    ]
  },
  "bindings": [
    {
      "agentId": "opus",
      "match": {
        "channel": "telegram",
        "peer": {
          "kind": "direct",
          "id": "123456789"
        }
      }
    },
    {
      "agentId": "chat",
      "match": {
        "channel": "telegram"
      }
    },
    {
      "agentId": "chat",
      "match": {
        "channel": "feishu"
      }
    },
    {
      "agentId": "chat",
      "match": {
        "channel": "whatsapp"
      }
    }
  ]
}
```

---

## 7.8.7 验证绑定

### CLI 命令

```bash
# 查看所有绑定
openclaw agents list --bindings

# 查看渠道状态
openclaw channels status --probe

# 测试消息路由
openclaw gateway call message.route --params '{"source":"telegram","peer":"123456789"}'
```

---

## 本节小结

1. **绑定规则**：将消息路由到指定 Agent
2. **优先级**：精确匹配优先
3. **多账户**：支持 Discord/Telegram/WhatsApp 多账户
4. **群组**：支持 DM 和群组级别绑定
5. **验证**：使用 CLI 验证绑定

---

## 课后思考

1. 你需要绑定哪些渠道？
2. 是否需要多账户支持？
3. 如何设计优先级？
